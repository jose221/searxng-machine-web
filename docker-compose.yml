services:
  searxng:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: searxng
    ports:
      - "8123:8080"
    environment:
      # Apuntamos a la ruta que creamos en el Dockerfile
      - SEARXNG_SETTINGS_PATH=/opt/searxng_config/settings.yml
      
      # Conexión a Redis usando el nombre del contenedor
      - SEARXNG_VALKEY_URL=redis://:Gonzales220@redis_production:6379/0
      - SEARXNG_BASE_URL=http://84.247.137.97:8123/
      - SEARXNG_SECRET_KEY=Gonzales2026_Key_Secure
    restart: always
